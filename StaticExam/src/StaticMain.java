import java.util.Arrays;

import ShuffleEx.Shuffle;

public class StaticMain {

		public static void main(String[] args) {
			
			// 1~9까지 숫자를 랜덤하게 뒤섞어서 result에 담아서 출력
			int[] original =  {1,2,3,4,5,6,7,8,9};
			System.out.println(original.toString());System.out.println(Arrays.toString(original));
			
			
			

			int[] result = shuffle.excute(original);
			System.out.println(original.toString());System.out.println(Arrays.toString(result));
			
			
			
			
			// 1. original 길이만큼 반복
			// 2. 랜덤함수 사용
			// 3. 임시로 배열의 n번째 값을 담을 변수 선언
			// 4. 파라미터로 받은 배열의 n번째랑 랜덤으로 생성한 배열의 n값을 바꿔서 리턴
			
			//for (int i = 0; i < 배열.length; i++) {
				// 랜덤 범위를 배열 길이 내에서만 나오게  작성
				//int rnd = Math.random();
				
				// 임시로 배열의 i번째를 저장할 변수를
										//여기에
				// 배열의 위치를 바꾼다
				// 바꾸는 요령은 배열의[i] 에 랜덤으로 생성한 번호의 배열요소
				// 랜덤으로 생성한 배열 요소에 i번째에 임시 배열 저장 변수값 대입
			}
			
			
			
			
			
			
			
			
			
			
			
			
			
		}

}
